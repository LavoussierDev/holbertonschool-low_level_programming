#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints out lower case
 * alphabet.
 *
 * Return: Always 0
 */

int main()
{
	int ch;
	for(ch = 'a'; <= 'z'; ch++)
		putchar(ch);
	return (0);
}
